{
  "scripts": {
    "build": "tsc",
    "build:dissertation": "cd dissertation && lualatex --shell-escape yocto-cfa.tex && bibtex yocto-cfa && lualatex --shell-escape yocto-cfa.tex && lualatex --shell-escape yocto-cfa.tex",
    "test": "prettier --check 'src/**/*.ts' && jest",
    "test:watch": "tsc --watch & jest --watch"
  },
  "dependencies": {
    "esprima": "^4.0.1"
  },
  "devDependencies": {
    "@types/esprima": "^4.0.2",
    "@types/jest": "^24.0.15",
    "jest": "^24.8.0",
    "prettier": "^1.18.2",
    "typescript": "^3.5.3"
  },
  "jest": {
    "rootDir": "lib",
    "testEnvironment": "node",
    "verbose": true
  }
}
