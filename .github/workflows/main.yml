name: Main
on: push
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
      - name: Install Dependencies
        run: npm ci
      - name: Test
        run: npm test
