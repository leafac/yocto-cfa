---
title: Yocto-CFA
author: Leandro Facchinetti
subject: TODO
keywords: todo, todo, ...
date: August 2020
draft: true # TODO
---

<Helmet
  title={props.pageContext.frontmatter.title}
  meta={[
    { name: "author", content: props.pageContext.frontmatter.author },
    { name: "subject", content: props.pageContext.frontmatter.subject },
    { name: "keywords", content: props.pageContext.frontmatter.keywords },
    { name: "date", content: new Date().toISOString().split("T")[0] },
  ]}
  htmlAttributes={{ lang: "en" }}
  bodyAttributes={{
    "data-mode": props.pageContext.frontmatter.draft ? "draft" : "final",
  }}
/>

<header>
<div style={{ page: "title-page", lineHeight: 1.2, textAlign: "center" }}>
<div style={{ marginTop: "1.5in", textTransform: "uppercase", fontWeight: "bold", letterSpacing: "2pt" }}>{props.pageContext.frontmatter.title}</div>
<div style={{ marginTop: "1in" }}>by<br />{props.pageContext.frontmatter.author}</div>
<div style={{ marginTop: "1.5in" }}>A dissertation submitted to Johns Hopkins University<br />in conformity with the requirements for the degree of Doctor of Philosophy</div>
<div style={{ marginTop: "0.5in" }}>Baltimore, Maryland<br />{props.pageContext.frontmatter.date}</div>
<div style={{ marginTop: "0.5in" }} className="draft">{new Date().toISOString()}</div>
<style>{`
@page title-page {
  @bottom {
    content: normal;
  }
}
`}</style>
</div>

# Abstract

<p>{props.pageContext.frontmatter.subject}</p>

- **Primary Reader and Advisor:** Dr. Scott Fraser Smith.
- **Readers:** Dr. Zachary Eli Palmer and Dr. Matthew Daniel Green.

<style>{`
header {
  page: header;
}

@page header {
  @bottom {
    content: counter(page, lower-roman);
  }
}
`}</style>

</header>

<main>

<style>{`
main {
  counter-reset: page 1;
}
`}</style>

</main>

<footer></footer>

import { Helmet } from "react-helmet";
import { MDXProvider } from "@mdx-js/react";

export default class extends React.Component {
  constructor(props) {
    super(props);
    this.components = {};
  }
  render() {
    return (
      <MDXProvider components={this.components}>
        {this.props.children}
      </MDXProvider>
    );
  }
}

<style>{`
@import "../node_modules/typeface-pt-serif/index.css";
@import "../node_modules/typeface-pt-mono/index.css";
@import "../node_modules/katex/dist/katex.css";

@page {
  size: US-Letter;
  margin: 1in 1in 1in 1.5in;
  font: 10pt/1 "PT Serif", prince-no-fallback;
  @bottom {
    content: counter(page);
  }
}

body {
  font: 12pt/2 "PT Serif", prince-no-fallback;
  text-align: justify;
  hyphens: auto;
}

a {
  text-decoration: none;
  color: black;
}

code {
  font-family: "PT Mono", prince-no-fallback;
}

p + p {
  margin-top: -1.12em;
  text-indent: 2em;
}

h1,
h2,
h3 {
  line-height: 1.3;
}

h1 {
  break-before: page;
}

h2,
h3 {
  margin-top: 2em;
}

.heading-counter {
  margin-right: 0.5em;
}

.draft {
  font-size: 10pt;
  color: #bbb;
}

[data-mode="final"] .draft {
  display: none;
}

pre {
  font-size: 10pt;
  line-height: 1.2;
  white-space: pre-wrap;
}

.grvsc-line {
  display: block;
}

.grvsc-line-highlighted {
  background-color: #e0ffff;
}

.table-of-contents {
  list-style-type: none;
  margin: 0;
}

.table-of-contents a::after {
  content: leader(".  ") target-counter(attr(href), page);
}

.table-of-contents a[data-section="header"]::after {
  content: leader(".  ") target-counter(attr(href), page, lower-roman);
}
`}</style>
